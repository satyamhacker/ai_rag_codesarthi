From repo/ (Codebase)
Authentication & User Management:

"How does the login flow work in auth.py?"

"What does the hash_password function do?"

"How is user registration handled?"

"What is the purpose of user_service.py?"

"How does logout work?"

"What database is used for storing users?"

"How are passwords stored securely?"

"What modules are in this project and what do they do?"

From pdf_file/mysql_cheatsheet.pdf
Based on the actual parsed content (8 pages loaded), these will get great answers:

Basics:

"What are the MySQL numeric data types?"

"What is the difference between CHAR and VARCHAR?"

"How do I insert a NULL value in MySQL?"

Querying:

"How do I use WHERE with BETWEEN, IN, and LIKE?"

"What is the syntax for SELECT DISTINCT?"

"How do I use ORDER BY with multiple columns?"

Grouping & Aggregates:

"How does GROUP BY with HAVING work?"

"What aggregate functions are available in MySQL?"

Joins:

"What are the different types of JOINs in MySQL?"

"How do I do a FULL JOIN in MySQL?" (workaround using UNION)

"What is a self join?"

Advanced:

"How do I create and use a VIEW in MySQL?"

"How do transactions work in MySQL? What is COMMIT and ROLLBACK?"

"How do I create a user and grant privileges in MySQL?"

"How do I create an index for performance?"

Pro tip: Try cross-domain questions like "How does the auth system store passwords, and how is that similar to MySQL's user management?" – your RAG chain can pull from both sources at once!